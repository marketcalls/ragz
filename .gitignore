# Visual brainstorming mockups (superpowers companion)
.superpowers/

# OS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# Env (bootstrap only per foundation spec — never secrets)
.env

# Runtime data (KEK keyfile etc.) — never commit
backend/data/

# Loadtest pre-provisioned tokens — throwaway local artifact, never commit
deploy/loadtest/.tokens.json
backend/celerybeat-schedule*

# local scratch / unrelated (not part of Ragz)
anything-llm/
openui/
.claude/
backend/*.dev.log
